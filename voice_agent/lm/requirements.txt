# Voice agent — chat-VLM venv (step 2: text/vision -> French reply)
# Install with:
#   uv venv .venv_lm && uv pip install --python .venv_lm/bin/python -r lm/requirements.txt
#
# Uses Qwen3.5-4B (a vision-language model) via mlx-vlm, so Bilou can both chat
# and describe what its camera sees. Newer mlx is required, incompatible with
# the moshi_mlx pin used by the Kyutai TTS step, so they live in separate envs.

mlx-vlm>=0.6.3          # 0.6.3+ handles the Qwen3.5 MoE conversion

# Pin to avoid a recurring downgrade: transformers (pulled by mlx-vlm) needs
# huggingface-hub>=1.5; other deps sometimes drag it back to 1.3.x which breaks
# the import. Keep it explicit.
huggingface-hub>=1.5.0,<2.0
